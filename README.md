# APEX

Project roadmap: see `MASTER_PLAN.md`.

Desktop automation CLI (mouse/keyboard control): see `docs/CLI_CONTROL.md`.

CLI-only RAM integration roadmap: see `CLI_MASTER_PLAN.md`.

Agent onboarding and repository exploration notes: see `AGENT.md`.

Quick dry-run example:

```bash
python3 cli/pc_control.py click --x 500 --y 500
```
